app.controller('SignInCtrl', ['$scope', '$auth', '$location', function($scope, $auth, $location){
  $scope.login = function (configName) {
    $auth.submitLogin($scope.loginForm, {config: configName}).
     then(function(resp) {
       console.log(resp);

       $location.path('/');
     }).
     catch(function(resp) {
       console.log(resp);
     });
  };
}])