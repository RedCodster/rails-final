app.controller('ResultsIndexCtrl', ['$scope', '$http', '$location', '$routeParams', function($scope, $http, $location, $routeParams){
  $http.get('/users/' + $routeParams.user_id + '/results').success(function(response){
    console.log(response);
    $scope.results = response;
  });
}]);